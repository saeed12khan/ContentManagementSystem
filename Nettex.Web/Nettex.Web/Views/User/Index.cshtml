 

@{ ViewBag.Title = Model; }

@Html.Nettex().Header(settings => {
    settings.Nav = DemoMenu.GetDemoMenu();
    settings.ShowTopbar = false;
    settings.StickyType = HeaderStickyType.Shrink;
    settings.BorderBottom = HeaderBorderBottom.Light;
})

 
@Html.Nettex().Section().Size(SectionSize.Small).ContentTemplate(@<text>

    <div class="container">
        <div class="row">
            <div class="col-lg-8">

                @Html.Nettex().UserShortInfo().User(Convert.ToString(ViewBag.UserName)).AppendCssClass("mb-6")
                 

                @(Html.Nettex().BlogPosts(BlogPostViewName.ListBottomText)
                    .Pageable(5)
                    .FilterByUserNames(new string[] { Convert.ToString(ViewBag.UserName) }))

            </div>

            <div class="col-lg-4">
                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarSubscribeForm", new SubscriberModel())
                </div>

                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarFollowLinks")
                </div>

                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarCategories")
                </div>

                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarPopularPosts")
                </div>

                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarLatestPosts")
                </div>

                <div class="mb-9">
                    @Html.Nettex().Widget("SidebarLatestComments")
                </div>
            </div>

        </div>
    </div>

</text>)


@Html.Nettex().Footer()